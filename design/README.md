# Design source snapshot

Read-only snapshot of the Claude Design project `fcde1df2-3faa-4a80-ab24-8f90fcb3c513`
("Personal dashboard with React app"), file "Patrick Beasley.dc.html" + its `support.js` runtime.

This is the visual/UX source of truth for the v2 rebuild (public one-pager + private dashboard).
When writing or revising a spec for any view, read the actual design source (this snapshot, or the
live project via the claude-design MCP tools) — do not paraphrase from memory. The MCP returns file
bodies HTML-entity-escaped; decode exactly one level when re-snapshotting.
